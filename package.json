{
  "name": "website",
  "version": "1.0.0",
  "description": "This project sets up a basic web project with Twig and Tailwind scaffolding. For a local development environment see: https://github.com/texasbaptists/vagrant-template. I prefer making the website a submodule of the local development repo. That way it can be deployed to other environments.",
  "scripts": {
    "build": "npx tailwindcss-cli@latest build src/styles.css -o ./public/css/styles.css"
  },
  "dependencies": {
    "autoprefixer": "^10.2.5",
    "tailwindcss": "^2.1.2"
  }
}
